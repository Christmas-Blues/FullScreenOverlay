import XCTest
@testable import FullScreenOverlay

final class FullScreenOverlayTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(FullScreenOverlay().text, "Hello, World!")
    }
}
